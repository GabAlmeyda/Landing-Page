WHAT DO I NEED TO PUT ON THIS LANDING PAGE?

    * some logo in the header
* hamburguer menu
    * email input (required);
    * name input (required)
    * tel input (opcional);
    * checkbox input (required);
    * submit button;
    * eye-catching image;
    * topic blocks about the benefits of the signing up;